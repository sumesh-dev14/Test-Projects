const input = document.getElementById('input');
const addBtn = document.getElementById('add-btn');

addBtn.addEventListener("click" , function(){
        const inputs = input.value;
        if(inputs == ""){
            alert("enter a element to add");
            return;
        }
        

})